# MMRL head tracker dependencies (macOS / Apple Silicon)
bleak>=0.21        # BLE via CoreBluetooth (the metawear SDK does not work on macOS)
python-osc>=1.8    # OSC UDP output

# Optional: only for --vqf host-side fusion mode (also pulls in numpy)
# vqf>=2.0

